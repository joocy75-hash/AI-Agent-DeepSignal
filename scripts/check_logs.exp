#!/usr/bin/expect -f
# Check backend logs

set timeout 60
set password "Vc8,xn7j_fjdnNGy"

spawn ssh root@141.164.55.245 "cd /root/auto-dashboard && docker compose --env-file .env.production logs backend --tail=100"

expect {
    "*password:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
